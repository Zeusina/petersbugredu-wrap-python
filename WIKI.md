# Petersburgedu Wrap Wiki

## Overview

`petersburgedu_wrap` is a Python package for working with the Petersburg education portal API.

Current repository snapshot:
- Package name: `petersburgedu_wrap`
- Version: `0.0.1`
- Python: `>=3.7`

## Project structure

- `README.md` — краткое описание проекта
- `setup.py` — метаданные пакета и параметры установки
- `petersbugredu_wrap/__init__.py` — пустой файл инициализации пакета

## Public API

At the moment, the repository does not expose documented public methods or classes in the package.

### Package initialization

`petersbugredu_wrap/__init__.py` is currently empty, so importing the package does not provide any documented symbols yet.

## Module documentation

### `setup.py`

This file defines package metadata and installation settings:

- `readme()` — reads `README.md` and returns its contents as a string.
- `setup(...)` — configures package metadata for distribution.

Key configuration values:
- `name='petersburgedu_wrap'`
- `version='0.0.1'`
- `author='zeusina'`
- `description='This is module for work with petersburgedu website API'`
- `python_requires='>=3.7'`

## Notes for future API documentation

When methods are added to the package, it is recommended to document each one with:
- purpose
- parameters
- return value
- raised exceptions
- usage example

Suggested wiki pages:
- `Home`
- `Installation`
- `Authentication`
- `API Reference`
- `Examples`
- `Changelog`
